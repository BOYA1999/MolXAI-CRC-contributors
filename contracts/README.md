# Frozen contracts

The contract files define the data partitions, primary population, nested set family, loss, CRC selector, risk levels, outcomes, gates, and stop conditions. They are copied from the reviewed local experiment line and are included to keep the release package auditable.

- `data_contract.md`
- `experiment_contract.md`
- `analysis_contracts.md`
