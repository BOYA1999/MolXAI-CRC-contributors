# GitHub 更新材料：先解压，再分别使用

目标仓库：https://github.com/BOYA1999/MolXAI-CRC-contributors

这层 ZIP 是运输包，不是替代科学版本的新发布号。内含两个已经独立审计的原始 ZIP；没有文章、出版图或作者内部记录。本轮未操作远端仓库。

## 必须保留的两个原始附件

| 文件 | SHA-256 | 内容 |
| --- | --- | --- |
| molxai-crc-anonymous-v3.zip | 032b3adc582fca31cfddc88ac853a4960b89b72addb70917ad0368a7c6b605a4 | 265 文件，代码、合同、来源与聚合结果；汇总截至 V5 |
| molxai-crc-reproduction-addendum-v6.zip | 1912d2050f11a57b1e20ec93355dc97bc56307e3f357c5de0209cffb6ca7aeb5 | 20 文件，主实验干净重建配方、锁环境、来源与许可 |

V6 准备程序校验的是第一个 ZIP 的完整字节哈希。不能重新压缩它，也不能用 GitHub 自动生成的 Source code ZIP 替代。建议将上述两个原始文件各自上传为同一最终版本 Release 的附件。

## 仓库文件更新

1. 当前远端核查于 2026-09-05，main@bf1230ded486d9398ecd7c33218d80098d88fd70，是旧 v1 的修改副本。
2. 若更新可浏览的源码树，使用 v3 内部目录的内容；包括 .gitignore，避免浏览器选择文件时漏掉隐藏文件。按私有审计差异清单处理旧文件，不仅叠加新文件。
3. V6 附加包保持独立子目录或独立 Release 附件，不要把其内容直接覆盖 v3 根目录。两个包都包含自己的 README、DATA_SOURCES 和 MANIFEST。
4. 若改写 README、补入任何文件，必须按新的清单边界重新计算校验清单。旧 v3 清单只对应原始 v3 包，不代表新增附加目录也已经被该清单覆盖。
5. 为最终上传内容固定 tag/release，并核实两个原始 ZIP 附件均可下载且 SHA-256 匹配。可进一步永久归档获得本项目 DOI；未实际生成前不填写 DOI。

## 公开范围与限制

- MIT 仅适用于自有代码和文档；保留两个包的全部来源、第三方署名和派生数据许可说明。
- 不加入稿件、SI、出版图、作者核对表、原始分子表、逐分子输出、权重或私有运行日志。
- V6 附加包是主 B-XAIC/Graph Attribution 重建程序，不代表所有实验路线均在本次重新训练；也不包含已完成 V6 全部机器可读运行汇总。
- V3 遗留 all-alpha recovery 字段有已知定义限制；随 V6 DATA_SOURCES 告警一起阅读，不能将字节重放解释成公式验证。
- 干净重训不是历史数值的精确重放；部分风险与 family 排序有变化。公开运行时应保留差异与失败，不只公开通过项。

上传后再将真实版本地址同步到稿件可用性说明；目前只是提供待上传材料。
